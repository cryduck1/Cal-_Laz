<Window x:Class="Calculator.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Калькулятор" Height="600" Width="350"
        Background="#1A1A1A"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="NumberButton" TargetType="Button">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="BorderBrush" Value="#3D3D3D"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3D3D3D"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#505050"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="OperatorButton" TargetType="Button">
            <Setter Property="Background" Value="#4A4A4A"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="BorderBrush" Value="#5A5A5A"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5A5A5A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#6A6A6A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="EqualsButton" TargetType="Button">
            <Setter Property="Background" Value="#606060"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="BorderBrush" Value="#707070"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#707070"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#808080"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ZeroButton" TargetType="Button" BasedOn="{StaticResource NumberButton}">
            <Setter Property="Grid.ColumnSpan" Value="2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="30,0,0,0"/>
        </Style>
    </Window.Resources>

    <Grid Background="#1A1A1A">
        <Grid.RowDefinitions>
            <RowDefinition Height="120"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="20,40,20,20">
            <TextBlock x:Name="DisplayText" 
                      Text="0" 
                      FontSize="56" 
                      FontWeight="Light"
                      Foreground="#FFFFFF" 
                      HorizontalAlignment="Right" 
                      VerticalAlignment="Bottom"
                      TextAlignment="Right"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Grid.Column="0" Content="C" Style="{StaticResource NumberButton}" Click="Clear_Click"/>
            <Button Grid.Row="0" Grid.Column="1" Content="±" Style="{StaticResource NumberButton}" Click="PlusMinus_Click"/>
            <Button Grid.Row="0" Grid.Column="2" Content="%" Style="{StaticResource NumberButton}" Click="Percent_Click"/>
            <Button Grid.Row="0" Grid.Column="3" Content="÷" Style="{StaticResource OperatorButton}" Click="Divide_Click"/>
            <Button Grid.Row="1" Grid.Column="0" Content="7" Style="{StaticResource NumberButton}" Click="Number_Click"/>
            <Button Grid.Row="1" Grid.Column="1" Content="8" Style="{StaticResource NumberButton}" Click="Number_Click"/>
            <Button Grid.Row="1" Grid.Column="2" Content="9" Style="{StaticResource NumberButton}" Click="Number_Click"/>
            <Button Grid.Row="1" Grid.Column="3" Content="×" Style="{StaticResource OperatorButton}" Click="Multiply_Click"/>
            <Button Grid.Row="2" Grid.Column="0" Content="4" Style="{StaticResource NumberButton}" Click="Number_Click"/>
            <Button Grid.Row="2" Grid.Column="1" Content="5" Style="{StaticResource NumberButton}" Click="Number_Click"/>
            <Button Grid.Row="2" Grid.Column="2" Content="6" Style="{StaticResource NumberButton}" Click="Number_Click"/>
            <Button Grid.Row="2" Grid.Column="3" Content="−" Style="{StaticResource OperatorButton}" Click="Subtract_Click"/>
            <Button Grid.Row="3" Grid.Column="0" Content="1" Style="{StaticResource NumberButton}" Click="Number_Click"/>
            <Button Grid.Row="3" Grid.Column="1" Content="2" Style="{StaticResource NumberButton}" Click="Number_Click"/>
            <Button Grid.Row="3" Grid.Column="2" Content="3" Style="{StaticResource NumberButton}" Click="Number_Click"/>
            <Button Grid.Row="3" Grid.Column="3" Content="+" Style="{StaticResource OperatorButton}" Click="Add_Click"/>
            <Button Grid.Row="4" Grid.Column="0" Content="0" Style="{StaticResource ZeroButton}" Click="Number_Click"/>
            <Button Grid.Row="4" Grid.Column="2" Content="." Style="{StaticResource NumberButton}" Click="Decimal_Click"/>
            <Button Grid.Row="4" Grid.Column="3" Content="=" Style="{StaticResource EqualsButton}" Click="Equals_Click"/>
        </Grid>
    </Grid>
</Window>